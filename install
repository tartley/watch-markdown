#!/usr/bin/env bash

set -e

sudo apt install -y inotify-tools falkon pandoc

(
    this_dir=$(pwd)
    cd "$HOME/.local/bin"
    ln -sf "$this_dir/watch-markdown" .
)

config="$HOME/.config/watch-markdown"
mkdir -p "$config"
cp github.css "$config"

desktop-file-install --dir="$HOME/.local/share/applications/" watch-markdown.desktop
